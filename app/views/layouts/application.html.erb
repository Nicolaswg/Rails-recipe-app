<!DOCTYPE html>
<html>
  <head>
    <title>RailsRecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
		<% if user_signed_in? %>
		<nav class="navbar flex-row">
			<ul class="nav flex-row">
			<li><%= link_to 'Recipe App', root_path %></li>
			<li><%= link_to 'My food', user_foods_path(current_user.id)%></li>
			<li><%= link_to 'My recipe', user_recipes_path(current_user.id)%></li>
			<li><%= link_to 'Public recipe', public_recipes_path%></li>
			<li><%= link_to 'Log out', destroy_user_session_path, method: :delete%></li>
			</ul>
		</nav>
		<%end%>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
