<section class='recipe'>
  <div>
    <h2><%= link_to (recipe.name), user_recipe_path(recipe.user_id, recipe.id) %></h2>
    <div><%= button_to "Remove", user_recipe_path(recipe.user_id, recipe.id), method: :delete %></div>
  </div>
  <p><%=recipe.description%></p>
</section>
